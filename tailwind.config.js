/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.html',  // Include all HTML files in the src folder and subfolders
    './src/**/*.js',    // Include all JavaScript files in the src folder and subfolders (if you are using JS)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

